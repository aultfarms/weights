{
  "name": "aultfarms-lib",
  "version": "0.0.1",
  "description": "Libraries for mapping data models in Ault Farms",
  "main": "index.js",
  "scripts": {
    "build": "babel --source-maps true --out-dir . src/",
    "watch": "babel --source-maps true --watch --out-dir . src/",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "lodash": "^4.17.5",
    "moment": "^2.21.0"
  },
  "devDependencies": {
    "babel-cli": "^6.26.0",
    "babel-core": "^6.26.0",
    "babel-eslint": "^8.2.2",
    "babel-preset-env": "^1.6.1"
  },
  "peerDependencies": {
    "cerebral": "^4.1.0",
    "bluebird": "^3.5.1"
  }
}
